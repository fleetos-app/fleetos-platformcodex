export default function LoginPage() {
  return (
    <main className="foundation-shell">
      <section className="foundation-panel" aria-labelledby="login-title">
        <p className="eyebrow">FleetOS</p>
        <h1 id="login-title">Authentication required</h1>
        <p className="summary">
          Supabase Auth is wired for this foundation. Product sign-in screens can
          be added when the application layer begins.
        </p>
      </section>
    </main>
  );
}
